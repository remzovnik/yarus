export interface IDtoVideoSource {
  id: number;
  name: string;
  color: string;
  active: boolean;
  isSelected: boolean;
  image: string;
  watermark: string;
  webIconActive: string;
  webIconInactive: string;
}
