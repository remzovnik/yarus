export interface ILogoConfigItem {
  startTime: [number, number, number, number, number, number];
  endTime: [number, number, number, number, number, number];
  urlLogo: string;
  urlLogoWhite: string;
  alt: string;
}
