export enum EValidationFileTypes {
  VIDEO = "video",
  CLIP = "clip",
  IMAGE = "image",
}

export enum EValidationFileResult {
  OK = "OK",
  INVALID_FORMAT_ERROR = "invalid_format",
  INVALID_SIZE_ERROR = "invalid_size",
}
