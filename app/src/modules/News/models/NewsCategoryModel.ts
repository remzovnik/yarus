import { BaseViewModel } from "@/_core/models/BaseViewModel";

export default class NewsCategoryModel extends BaseViewModel {
  color: string;
  enable: boolean;
  icon: string;
  id: number;
  image: string;
  name: string;
}
